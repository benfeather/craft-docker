{
    "name": "Craft Docker Test",
    "dockerComposeFile": "../docker-compose.yml",
    "service": "php",
    "runServices": ["web", "php", "database", "redis"],
    "forwardPorts": [8000],
    "workspaceFolder": "/var/www/html/",
    "settings": {
        "terminal.integrated.shell.linux": "/bin/ash"
    }
}